(function(){
  var isCollection, head, tail, last, asCollection;
  isCollection = function(a){
    return a && !('nodeType' in a) && a.length >= 0;
  };
  head = function(xs){
    return xs[0];
  };
  tail = function(xs){
    var i$, len$, i, x, results$ = [];
    for (i$ = 0, len$ = xs.length; i$ < len$; ++i$) {
      i = i$;
      x = xs[i$];
      if (i > 0) {
        results$.push(x);
      }
    }
    return results$;
  };
  last = function(xs){
    var ref$;
    return (ref$ = asCollection(xs))[ref$.length - 1];
  };
  asCollection = function(xs){
    switch (false) {
    case !isCollection(xs):
      return xs;
    default:
      return [xs];
    }
  };
  module.exports = {
    head: head,
    tail: tail,
    last: last,
    asCollection: asCollection
  };
}).call(this);
